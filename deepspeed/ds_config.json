{
  "train_batch_size": 32,
  "steps_per_print": 10000,
  "optimizer": {
    "type": "SGD",
    "params": {
      "lr": 0.1,
      "momentum": 0.9,
      "weight_decay": 5e-4
    }
  },
  "scheduler": {
    "type": "WarmupLR",
    "params": {
      "warmup_min_lr": 0,
      "warmup_max_lr": 0.001,
      "warmup_num_steps": 1000
    }
  },
  "wall_clock_breakdown": false
}
